import { DOCS } from './models.js?v=4';
import { icon, esc } from './ui.js?v=3';

const docHref=id=>id==='overview'?'#/docs':`#/docs/${id}`;
const docLink=(id,label,cls='')=>`<a href="${docHref(id)}" class="${cls}">${label} ${icon('arrow')}</a>`;
const callout=(title,body)=>`<aside class="doc-callout">${icon('spark')}<div><strong>${title}</strong><p>${body}</p></div></aside>`;
function codeBlock(title,code,download) { return `<div class="doc-code"><div><span>${icon('file')}${title}</span><button data-copy-global="${esc(code)}" aria-label="复制 ${title}">${icon('copy')}复制</button></div><pre><code>${esc(code)}</code></pre>${download?`<a class="code-download" href="downloads/${download}" download>${icon('download')} 下载 ${download}</a>`:''}</div>`; }

export function docBody(id) {
  if(id==='overview')return `<section class="docs-welcome"><p class="eyebrow"><span class="live-dot"></span> THE DOCUMENTATION</p><h1>让工具就位，<br>让<span class="gradient-text">研究开始。</span></h1><p>连接模型与客户端，准备材料与 Skills。<br>按你现在要做的事，找到下一步。</p><a class="button button-dark" href="#/docs/codex">阅读 Codex 安装教程 ${icon('arrow')}</a><div class="welcome-symbol" aria-hidden="true">${icon('book')}<span>DOCS / 01</span></div></section><section id="getting-started" class="doc-section"><div class="doc-section-title"><h2>快速开始</h2><span>START HERE</span></div><a href="#/docs/codex" class="featured-doc"><span class="featured-doc-icon">${icon('terminal')}</span><div><span class="small-tag">可视化教程</span><h3>Codex 安装与配置</h3><p>从下载安装到首次对话，跟着 6 个步骤完成连接。</p><span class="platform-tags"><span>Windows</span><span>macOS</span><span>交互演示</span></span></div><span class="featured-doc-arrow">${icon('arrow')}</span></a><div class="doc-card-grid">${DOCS.filter(d=>!['overview','codex'].includes(d.id)).map((d,i)=>`<a href="${docHref(d.id)}" class="doc-card"><span class="doc-card-top">${icon(d.icon)}<small>0${i+2}</small></span><h3>${d.title}</h3><p>${d.summary}</p><span class="doc-card-link">阅读文档 ${icon('arrow')}</span></a>`).join('')}</div></section><section class="learning-path doc-section" id="learning-path"><div class="doc-section-title"><h2>一条清晰的上手路径</h2><span>YOUR FIRST CONNECTION</span></div><div class="path-stops">${[['01','认识模型','按分组比较价格，选择任务所需的模型。','#/models'],['02','准备密钥','注册账号，创建自己的 Key。','#/docs/keys'],['03','连接工具','完成配置，开始首次对话。','#/docs/codex']].map(([n,t,d,url])=>`<a href="${url}"><b>${n}</b><h3>${t} ${icon('arrow')}</h3><p>${d}</p></a>`).join('')}</div></section>`;
  if(id==='skills')return `<div class="article-heading"><p class="eyebrow">RESEARCH SKILLS <span>/ GET STARTED</span></p><h1>把一个 Skill 用起来</h1><p>从熟悉的任务开始。下载方法文件，带上材料，让工作按清楚的顺序展开。</p><div class="article-meta"><span>${icon('layers')}8 个科研 Skills</span><a href="downloads/token-research-skills.zip" download>${icon('download')}下载完整合集</a></div></div><div class="prose"><section id="choose-skill"><h2>01. 选一个眼前的任务</h2><p>准备读一组论文，可以从「文献地图」开始；想整理实验结果，可以选择「科研图表」。每个 Skill 页面都写有需要准备的材料、工作步骤和预期产物。</p><div class="doc-resource-grid"><a href="#/skills/literature-map"><h3>文献地图</h3><p>比较问题、方法、假设和证据，整理下一步阅读方向。</p><span>查看 Skill →</span></a><a href="#/skills/research-figures"><h3>科研图表</h3><p>先明确一张图要回答什么，再整理数据、样式和导出要求。</p><span>查看 Skill →</span></a></div></section><section id="unpack-skill"><h2>02. 下载并解压</h2><p>在 Skill 页面点击「下载 Skill」。解压后会得到一个以方法命名的文件夹，其中的 <code>SKILL.md</code> 包含完整工作方法。它是纯文本 Markdown 文件，可以直接打开阅读。</p>${codeBlock('文件结构','literature-map/\n  SKILL.md')}<p>下载完整合集时，每个 Skill 都在各自的文件夹中。先选一个与你当前任务最接近的方法即可。</p></section><section id="use-skill"><h2>03. 交给你正在使用的 AI 客户端</h2><ol class="numbered-list"><li><div><strong>让客户端读到方法</strong><p>在支持文件附件的对话中添加 SKILL.md；也可以打开文件，把全文复制到对话里。在能读取项目文件的客户端中，告诉它 SKILL.md 的具体位置。</p></div></li><li><div><strong>补齐研究材料</strong><p>按 Skill 页面准备论文、数据、代码或草稿。先确认文件能够打开，再说明这次要完成的任务，以及希望产物保存在哪里。</p></div></li><li><div><strong>给出第一个任务</strong><p>复制 Skill 页面上的「从这句话开始」，替换成你自己的研究问题。让客户端先复述材料、目标和计划，再按步骤推进。</p></div></li></ol>${codeBlock('任务示例','请读取我提供的 SKILL.md，按其中的文献地图方法工作。\n材料：本次上传的论文与阅读笔记。\n目标：比较这些研究使用的关键假设与证据。\n请先列出材料清单与整理计划，再开始比较。')}</section><section id="keep-method"><h2>04. 留下适合自己的版本</h2><p>完成第一次任务后，打开 SKILL.md，补充你的领域术语、常用表格列名、图表风格或交付检查项。下一次研究就能沿用这些习惯。</p><p>给修改后的方法写一条简短记录：改了哪里、为什么改、在哪个任务中用过。团队共享时，附上一份输入材料说明和产物样例。</p><a class="inline-doc-link" href="#/gallery">去 Gallery 看完整工作流 ${icon('arrow')}</a></section></div>`;
  if(id==='research-files')return `<div class="article-heading"><p class="eyebrow">RESEARCH WORKFLOW <span>/ MATERIALS</span></p><h1>让材料跟着研究走</h1><p>一套容易找到、容易接着做的目录，比复杂的管理系统更适合起步。</p></div><div class="prose"><section id="project-structure"><h2>从一个清楚的项目目录开始</h2><p>围绕一个研究问题组织材料。让输入、工作记录和最终产物有各自的位置。</p>${codeBlock('一个可直接沿用的目录','research-project/\n  README.md          # 当前问题、项目入口与下一步\n  papers/            # 论文、来源与阅读笔记\n  data/              # 数据入口、说明与处理记录\n  code/              # 脚本、环境与运行方法\n  experiments/       # 每次运行的配置、日志与产物\n  figures/           # 图表代码、源数据与导出文件\n  writing/           # 草稿、意见与修改记录\n  skills/            # 项目使用的方法文件')}</section><section id="task-context"><h2>每个任务，带上四样东西</h2><div class="check-grid">${[['问题','这次想回答什么，做到哪一步算完成。'],['材料','相关论文、数据、代码的具体入口。'],['方法','选用的 Skill、已有规范和评价口径。'],['产物','希望得到的文件、格式和保存位置。']].map(([t,d])=>`<div>${icon('check')}<strong>${t}</strong><p>${d}</p></div>`).join('')}</div><p>把这些内容写进任务说明，能减少后续反复补充上下文的时间。</p></section><section id="leave-trace"><h2>结束时，为下次工作留一个入口</h2><p>在 README 中记录已完成的工作、主要产物位置、仍未解决的问题，以及建议的下一步。实验任务附上配置、代码版本、运行命令与日志位置。</p><div class="doc-resource-grid"><a href="#/skills/experiment-ledger"><h3>实验记录 Skill</h3><p>把每次运行的设置与产物接在一起。</p><span>查看方法 →</span></a><a href="#/skills/lab-handoff"><h3>研究交接 Skill</h3><p>让下一个接手的人知道从哪里继续。</p><span>查看方法 →</span></a></div></section></div>`;
  if(id==='pricing')return `<div class="article-heading"><p class="eyebrow">MODEL PRICING <span>/ GUIDE</span></p><h1>读懂模型价格</h1><p>先选择分组，再比较输入、输出与缓存的人民币单价。</p></div><div class="prose"><section id="pricing-units"><h2>卡片上的数字表示什么</h2><div class="reference-table">${[['输入','发送给模型的提示词、对话历史和材料所产生的输入词元。'],['输出','模型回复所产生的输出词元。'],['缓存读取','请求命中缓存时，对应读取部分的词元单价。'],['缓存写入','模型支持缓存写入计费且已录入价格时，对应写入部分的词元单价。']].map(([a,b])=>`<div><code>${a}</code><p>${b}</p></div>`).join('')}</div><p>按词元计费的模型统一展示 <strong>人民币 / 100 万词元（1M tokens）</strong>。按次计费的模型在卡片上单独标注；「—」表示暂无对应价格记录，「待设置」表示还未录入价格。</p></section><section id="pricing-groups"><h2>同一个模型，为什么会有不同分组</h2><p>每个分组包含一组模型和自己的倍率。选择分组后，卡片会显示这一分组对应的价格。</p>${codeBlock('换算规则','人民币单价 = 官方美元单价 ÷ 换算系数 × 分组倍率')}<p>换算系数的初始设置为 <strong>35</strong>。以某项官方单价 $3.50 为例：倍率 1 时是 ¥0.10；倍率 1.5 时是 ¥0.15。模型的输入、输出与缓存分别使用同一规则换算。</p></section><section id="estimate-usage"><h2>估算一次任务的词元费用</h2><p>将实际输入与输出词元数量分别除以 100 万，再乘上各自单价。存在缓存时，按对应缓存单价计算缓存部分，未缓存部分使用输入单价。</p>${codeBlock('无缓存示例','假设输入单价 ¥0.10 / 1M，输出单价 ¥0.50 / 1M\n输入 20,000 词元：20,000 ÷ 1,000,000 × 0.10 = ¥0.002\n输出 5,000 词元：5,000 ÷ 1,000,000 × 0.50 = ¥0.0025\n合计：¥0.0045')}<p>这是理解计价方式的示例。模型广场会显示当前分组和模型的具体单价。</p><a class="button button-dark" href="#/models">按分组查看价格 ${icon('arrow')}</a></section></div>`;
  if(id==='codex')return `<div class="article-heading"><p class="eyebrow">QUICK START <span>/ 01</span></p><h1>Codex 安装与配置</h1><p>从下载安装到第一次对话，跟着 6 步完成。<br>先选择系统，再按图示在你的电脑上操作。</p><div class="article-meta"><span>${icon('terminal')}桌面客户端</span><span>Windows / macOS</span><a href="downloads/codex-config.zip" download>${icon('download')}配置文件包</a></div></div><div id="tutorial-mount"></div><div class="article-related">接下来，你还可以阅读 ${docLink('config','配置文件说明')}</div>`;
  if(id==='keys')return `<div class="article-heading"><p class="eyebrow">QUICK START <span>/ 02</span></p><h1>账号与 API 密钥</h1><p>让应用通过你的账户使用模型，从创建一枚 Key 开始。</p><div class="article-meta"><span>${icon('key')}账户设置</span><span>适用于 Windows 与 Mac</span></div></div><div class="prose"><section id="create-account"><h2>01. 注册并进入控制台</h2><p>打开词元智研，完成注册或登录后，进入控制台。这里是查看密钥、模型与账户使用情况的入口。</p><a class="button button-dark" href="https://tokenresearch.com.cn" target="_blank" rel="noopener noreferrer">打开词元智研 ${icon('northeast')}</a></section><section id="create-key"><h2>02. 创建 API 密钥</h2><ol class="numbered-list"><li><div><strong>打开「API 密钥」</strong><p>在控制台中找到 API 密钥管理页面，点击「创建密钥」。</p></div></li><li><div><strong>填写一个容易辨认的名称</strong><p>例如「我的 Codex」。根据你的账户选择可用分组，模型与额度以控制台显示为准。</p></div></li><li><div><strong>复制完整 Key</strong><p>使用复制按钮取得完整内容。列表中带省略号的缩略文本不能直接用于配置。</p></div></li></ol></section><section id="use-key"><h2>03. 把 Key 放进正确的位置</h2><p>如果你正在配置 Codex，将完整 Key 填入 <code>auth.json</code> 中 <code>OPENAI_API_KEY</code> 对应的值，再保存到个人目录下的 <code>.codex</code> 文件夹。</p>${codeBlock('auth.json','{\n  "OPENAI_API_KEY": "YOUR_API_KEY_HERE"\n}','auth.json')}${callout('Key 与登录密码不同','API Key 是应用调用服务使用的凭据。请填入自己的 Key，并保留在自己的配置文件中。')}${docLink('codex','跟着可视化教程完成配置','inline-doc-link')}</section><section id="manage-key"><h2>密钥需要更换时</h2><p>在控制台创建新的 Key，更新应用中的配置，确认能够正常使用后，再停用不再需要的旧密钥。若密钥意外公开，应尽快在控制台停用并重新创建。</p></section></div>`;
  if(id==='config')return `<div class="article-heading"><p class="eyebrow">INTEGRATION REFERENCE <span>/ 01</span></p><h1>配置文件说明</h1><p>两个文件，各司其职。连接设置与账户密钥都放在这里。</p><div class="article-meta"><span>${icon('file')}Codex 配置参考</span><a href="downloads/codex-config.zip" download>${icon('download')}下载完整配置包</a></div></div><div class="prose"><section id="file-location"><h2>文件放在哪里</h2><p>将解压得到的两个文件直接放在当前用户的 <code>.codex</code> 文件夹中。</p><div class="location-table"><div><span>Windows</span><code>%userprofile%\\.codex</code><button data-copy-global="%userprofile%\\.codex" aria-label="复制 Windows 配置目录">${icon('copy')}</button></div><div><span>macOS</span><code>~/.codex</code><button data-copy-global="~/.codex" aria-label="复制 Mac 配置目录">${icon('copy')}</button></div></div>${docLink('codex','查看如何找到隐藏文件夹','inline-doc-link')}</section><section id="connection-config"><h2>config.toml：连接设置</h2><p>配置包已写好词元智研的服务地址和模型设置。下面是其中与模型连接相关的部分。</p>${codeBlock('config.toml · 连接部分','model = "gpt-6-astra"\nmodel_provider = "me"\n\n[model_providers.me]\nname = "me"\nbase_url = "https://tokenresearch.com.cn"\nwire_api = "responses"\nrequires_openai_auth = true','config.toml')}<div class="reference-table"><div><code>model</code><p>要使用的模型名称。配置包沿用教程设置，实际可用模型以控制台为准。</p></div><div><code>base_url</code><p>词元智研的服务地址。</p></div><div><code>wire_api</code><p>与服务通信所用的 API 形式，当前配置为 responses。</p></div></div></section><section id="auth-config"><h2>auth.json：你的 API 密钥</h2><p>只替换占位文字 <code>YOUR_API_KEY_HERE</code>，保留大括号、冒号和英文双引号。</p>${codeBlock('auth.json','{\n  "OPENAI_API_KEY": "YOUR_API_KEY_HERE"\n}','auth.json')}<p>保存时确认是纯文本，文件名为 <code>auth.json</code>，没有额外的 <code>.txt</code> 或 <code>.rtf</code> 后缀。</p></section><section id="apply-config"><h2>让修改生效</h2><p>已有配置时，先备份原文件再替换。保存后完全退出 Codex，然后重新打开，发起一次对话确认连接。</p>${callout('配置包与当前账户配合使用','模型是否可用、密钥分组和账户额度，请在词元智研控制台中核对。')}${docLink('troubleshooting','遇到问题？查看排查步骤','inline-doc-link')}</section></div>`;
  return `<div class="article-heading"><p class="eyebrow">INTEGRATION REFERENCE <span>/ 02</span></p><h1>常见问题与排查</h1><p>从最常见的几个原因开始，一项一项确认。</p><div class="article-meta"><span>${icon('help')}连接与配置</span><span>按问题查找</span></div></div><div class="prose"><section id="quick-check"><h2>先做这四项检查</h2><div class="check-grid">${[['目录正确','两个文件在当前用户的 .codex 内。'],['文件名正确','没有多余的 .txt、.rtf 或下载序号。'],['密钥完整','已替换占位文字，保留英文引号。'],['完全重启','保存文件后，退出并重新打开 Codex。']].map(([t,p])=>`<div>${icon('check')}<strong>${t}</strong><p>${p}</p></div>`).join('')}</div></section><section id="common-issues"><h2>按你看到的情况排查</h2><div class="doc-faq">${[
    ['找不到 .codex 文件夹','先打开一次 Codex 再退出。Windows 可用 Win + R 输入 %userprofile%\\.codex；Mac 用访达「前往文件夹」输入 ~/.codex。Mac 的隐藏文件可以用 Command + Shift + 英文句点显示。'],
    ['重启后仍然停在登录页面','检查配置是否位于当前系统账户的 .codex 中，两个文件是否直接放在目录内，auth.json 是否已经填入完整 Key。之后完全退出客户端再重新打开。'],
    ['提示 401 或密钥无效','从词元智研控制台重新复制完整 Key，确认密钥未被停用或过期。检查是否误填了登录密码、缩略内容、占位文字或多余引号。'],
    ['提示模型不可用或账户无额度','进入控制台，检查密钥所属分组、当前可用模型和账户额度。配置包沿用教程模型设置，若服务方调整模型，请按控制台信息更新。'],
    ['一直等待，没有收到回复','检查网络和服务状态，对照下载包确认服务地址没有写错。重新保存配置并完全重启；如仍然失败，记录错误信息后联系服务方。'],
    ['Mac 保存后，文件变成 .rtf 或 .txt','在文本编辑中使用「格式 → 制作纯文本」，并关闭自动添加扩展名。也可用安装教程第五步的工具生成 auth.json，下载后确认名称再替换。'],
  ].map(([q,a])=>`<details><summary>${q}<span>+</span></summary><p>${a}</p></details>`).join('')}</div></section><section id="try-again"><h2>重新对照完整流程</h2><p>如果不确定遗漏了哪一步，可以回到可视化教程。点击步骤目录就能直接跳转，当前浏览器会记住你的教程进度。</p>${docLink('codex','打开 Codex 安装教程','button button-dark')}</section></div>`;
}

export const toc = {
 skills:[['choose-skill','选择任务'],['unpack-skill','下载与解压'],['use-skill','开始使用'],['keep-method','留下自己的方法']],
 'research-files':[['project-structure','项目目录'],['task-context','任务上下文'],['leave-trace','留下工作入口']],
 pricing:[['pricing-units','价格单位'],['pricing-groups','模型与分组'],['estimate-usage','估算任务费用']],
 overview:[['getting-started','快速开始'],['learning-path','上手路径']],
 keys:[['create-account','注册与登录'],['create-key','创建 API 密钥'],['use-key','使用密钥'],['manage-key','更换密钥']],
 config:[['file-location','文件位置'],['connection-config','连接设置'],['auth-config','账户密钥'],['apply-config','让修改生效']],
 troubleshooting:[['quick-check','四项快速检查'],['common-issues','按现象排查'],['try-again','重新对照流程']],
};
